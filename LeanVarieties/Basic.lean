import Mathlib.AlgebraicGeometry.Over
import Mathlib.AlgebraicGeometry.Morphisms.FiniteType
import Mathlib.AlgebraicGeometry.Morphisms.Separated

/-!
# Basic varieties infrastructure

This module is intentionally only a compile-time smoke test for the mathlib
scheme-theoretic primitives that the varieties API will build on.

The actual `Variety` abstraction is deferred until the representation decision
in `docs/0001-variety-semantics.md` has been reviewed.
-/

namespace LeanVarieties

end LeanVarieties
